@startuml A002-sequence

title A002 員工訂餐與菜單管理 - 序列圖

actor 員工
actor 系統管理員
participant 前端 as FE
participant 後端 as BE
database 資料庫 as DB

== 管理員建立菜單 ==
系統管理員 -> FE : 輸入便當資訊與有效期間
FE -> BE : POST /api/menus（JWT Token）
BE -> BE : 驗證管理員身份
BE -> DB : 儲存菜單資料
DB --> BE : 確認儲存成功
BE --> FE : 回傳建立成功
FE --> 系統管理員 : 顯示建立成功

== 管理員查詢菜單清單 ==
系統管理員 -> FE : 進入菜單管理頁面
FE -> BE : GET /api/menus（JWT Token）
BE -> DB : 查詢菜單清單
DB --> BE : 回傳菜單清單
BE --> FE : 200 回傳菜單清單
FE --> 系統管理員 : 顯示菜單清單

== 管理員編輯菜單 ==
系統管理員 -> FE : 選取菜單並修改欄位
FE -> BE : PATCH /api/menus/{id}（JWT Token）
BE -> DB : UPDATE menus WHERE id = {id}
DB --> BE : 更新成功
BE --> FE : 200 編輯成功
FE --> 系統管理員 : 顯示編輯成功

== 員工瀏覽並訂餐 ==
員工 -> FE : 進入訂餐頁面
FE -> BE : GET /api/orders/menu（JWT Token）
BE -> BE : 檢查當週星期五 12:00 截止時間
alt 已截止（星期五 12:00 後）
  BE --> FE : 回傳截止提示
  FE --> 員工 : 顯示截止提示，僅可查看記錄
else 未截止
  BE -> DB : 查詢截止日後有效的菜單（JOIN suppliers，不限週一至週五）
  DB --> BE : 回傳菜單清單（含供應商名稱）
  BE --> FE : 回傳菜單清單（id、name、category、supplier_name，不含 price）
  FE --> 員工 : 顯示便當選項（格式：便當名稱 / 種類 / 廠商名稱）
  員工 -> FE : 選擇日期與便當
  FE -> BE : POST /api/orders（JWT Token）
  BE -> DB : 查詢該員工該日是否已有訂單
  alt 已有訂單
    DB --> BE : 回傳現有訂單
    BE -> DB : UPDATE orders（覆蓋為新便當）
    DB --> BE : 更新成功
    BE --> FE : 200 訂餐成功（覆蓋）
  else 尚無訂單
    BE -> DB : INSERT orders
    DB --> BE : 新增成功
    BE --> FE : 201 訂餐成功
  end
  FE --> 員工 : 顯示訂餐成功
end

== 員工更新訂餐 ==
員工 -> FE : 選擇已訂便當並更換
FE -> BE : PATCH /api/orders/{id}（JWT Token）
BE -> BE : 檢查截止時間（星期五 12:00）
alt 已截止
  BE --> FE : 回傳截止提示
  FE --> 員工 : 顯示無法更新
else 未截止
  BE -> DB : UPDATE orders SET menu_id = 新便當
  DB --> BE : 更新成功
  BE --> FE : 200 更新成功
  FE --> 員工 : 顯示更新成功
end

== 員工取消訂餐（前後端皆驗證） ==
員工 -> FE : 進入訂餐記錄頁面
FE -> FE : 依各訂單訂餐日判斷是否已過前一日 16:30
note right of FE
  已超過截止時間：隱藏取消按鈕
  未超過截止時間：顯示取消按鈕
end note
FE --> 員工 : 顯示訂餐記錄（取消按鈕依截止狀態顯示或隱藏）

員工 -> FE : 點擊取消按鈕
FE -> FE : 再次確認當前時間是否在截止時間前
FE -> BE : DELETE /api/orders/{id}（JWT Token）
BE -> BE : 驗證是否在訂餐日前一日 16:30 前
alt 已超過截止時間
  BE --> FE : 403 已超過截止時間
  FE --> 員工 : 顯示錯誤提示
else 未超過截止時間
  BE -> DB : 刪除訂餐記錄
  DB --> BE : 確認刪除成功
  BE --> FE : 200 取消成功
  FE --> 員工 : 顯示取消成功
end

== 員工查看個人訂餐記錄 ==
員工 -> FE : 進入個人訂餐記錄頁面
FE -> BE : GET /api/orders/me（JWT Token）
BE -> DB : 查詢該員工所有訂餐記錄
DB --> BE : 回傳訂餐記錄清單
BE --> FE : 200 回傳訂餐記錄清單
FE --> 員工 : 顯示個人訂餐記錄

== 管理員新增供應商 ==
系統管理員 -> FE : 輸入供應商資料（名稱、Email、電話、負責人、營業登記編號）
FE -> BE : POST /api/suppliers（JWT Token）
BE -> BE : 驗證管理員身份
BE -> DB : INSERT suppliers
DB --> BE : 新增成功
BE --> FE : 201 新增成功
FE --> 系統管理員 : 顯示新增成功

== 管理員取消員工訂餐 ==
系統管理員 -> FE : 選擇取消指定員工訂餐
FE -> BE : DELETE /api/admin/orders/{id}（JWT Token）
BE -> BE : 驗證管理員身份
BE -> BE : 檢查是否在訂餐日前一日 16:30 前\n（批次排程執行前 30 分鐘）
alt 已超過截止時間（16:30 後）
  BE --> FE : 403 已超過截止時間
  FE --> 系統管理員 : 顯示截止提示，無法取消
else 未超過截止時間
  BE -> DB : 刪除訂餐記錄
  DB --> BE : 確認刪除成功
  BE --> FE : 回傳取消成功
  FE --> 系統管理員 : 顯示取消成功
end

@enduml
