@startuml A002-activity

title A002 員工訂便當 - 活動圖

start

:員工登入後進入訂餐頁面;
:系統檢查是否在截止時間前（星期五 12:00）;

if (尚未截止?) then (否)
  :顯示截止提示;
  :僅顯示訂餐記錄;
  stop
else (是)
  :顯示下週工作日便當選項;
  :員工選擇日期與便當;
  :系統儲存訂餐記錄;

  if (員工想取消訂餐?) then (是)
    :系統檢查是否在截止時間前;
    if (尚未截止?) then (是)
      :系統刪除訂餐記錄;
    else (否)
      :顯示截止提示，無法取消;
    endif
  endif
  stop
endif

@enduml
