@startuml A002-activity

title A002 員工訂便當 - 活動圖

fork
  :員工登入後進入訂餐頁面;
  :系統檢查是否在當週星期五 12:00 前;
  if (尚未截止?) then (否)
    :顯示截止提示，僅顯示訂餐記錄;
    stop
  endif
  :顯示下週（僅下週）工作日便當選項;
  :員工選擇日期與便當;
  :系統檢查該日是否已有訂單;
  if (已有訂單?) then (是)
    :覆蓋舊訂單（以最新一筆為準）;
  else (否)
    :新增訂餐記錄;
  endif
  stop

fork again
  :員工選擇已訂便當並更換;
  :系統檢查是否在截止時間前（星期五 12:00）;
  if (尚未截止?) then (是)
    :更新訂餐記錄為新選擇的便當;
    :顯示更新成功;
  else (否)
    :顯示截止提示，無法更新;
  endif
  stop

fork again
  :員工選擇欲取消的訂單;
  :系統檢查是否在截止時間前（星期五 12:00）;
  if (尚未截止?) then (是)
    :系統刪除訂餐記錄;
    :顯示取消成功;
  else (否)
    :顯示截止提示，無法取消;
  endif
  stop

fork again
  :管理員進入菜單管理頁面;
  fork
    :查詢菜單清單\n（帶 include_history 可含歷史菜單）;
    stop
  fork again
    :選取菜單並編輯欄位;
    :系統更新菜單資料;
    :顯示編輯成功;
    stop
  end fork

fork again
  :管理員選擇欲取消的員工訂單;
  :系統檢查是否在訂餐日前一日 16:30 前\n（批次排程執行前 30 分鐘）;
  if (尚未截止?) then (是)
    :系統刪除指定訂餐記錄;
    :顯示取消成功;
  else (否)
    :顯示截止提示，無法取消;
  endif
  stop

end fork

@enduml
